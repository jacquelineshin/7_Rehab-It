Week 1 Progress 9/14

- Created the Django project and initial project structure.
- Implemented split settings for development and production.
- Added environment variable support using a .env file.
- Added .env.example and configured .gitignore to prevent .env from being committed.
- Connected the local project to the GitHub repository.
- Resolved the initial Git merge conflict.
- Next steps: create and test the development and production configurations and continue building the project.

Week 2 Progress 9/22
- Implemented data aggregation using the Django ORM to calculate the number of exercises at each difficulty level.
- Created a Django endpoint that returns the aggregated exercise data as JSON.
- Used Matplotlib to generate a bar chart showing the number of exercises by difficulty.
- Added a chart title, axis labels, and legend to meet the visualization requirements.
- Created a Django HTML template to display the generated chart.
- Created an image endpoint that serves the chart as a PNG using HttpResponse.
- Updated the chart generation to use BytesIO so the PNG can be generated and served directly from memory rather than saved to disk.
- Learned about RAM implications, including how larger charts and multiple simultaneous requests can increase memory usage.
- Configured a project-level static/ directory for Django static files.
- Added STATIC_URL and STATICFILES_DIRS to the project settings.
- Verified Django could locate static files using python manage.py findstatic.
- Tested the configuration successfully and confirmed Django can load files from the project-level static directory.